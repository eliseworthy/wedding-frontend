.row
  .twelvecol.last
    #callout
      h1#tag Bridle your Wedding Plans!
      #sign-up
        = button_to "Sign Up >", new_user_path, class: "callout-button", method: "get"
        #browse
          = link_to "or Browse Ideas", pins_path
.row
  #wedding-ideas
    .bar-list.ninecol
      h1#tag Recent Weddings
      - @weddings.each do |wedding|
        = render "weddings/wedding", { wedding: wedding }
    #search
      h1#tag Collect Ideas
      .side-nav.threecol.last
        = button_to "Search for Honeymoons!", honeymoons_path, class: "little-button", method: "get"
        = form_tag(pins_path, method: "get") do
          = text_field_tag(:search)
          = button_tag "Search for Wedding Ideas", class: "little-button"

